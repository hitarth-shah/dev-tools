import { Heading } from "@/components/headings";

const Cron = () => {
  return (
    <div className="flex flex-col p-8 h-screen w-full max-w-[1600px] mx-auto">
      <Heading
        heading="CRON Calculator"
        subHeading="Generate and understand cron expressions"
      />
    </div>
  );
};

export default Cron;
